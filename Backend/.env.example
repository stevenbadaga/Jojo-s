# Example environment variables for Kitenge Bora Backend
#
# IMPORTANT:
# - Do NOT put real secrets in this file if you plan to commit it.
# - For local dev, copy this to `.env` (which is gitignored) OR set the env vars in your shell.
# - Spring Boot does not automatically load `.env` files unless you add a dotenv loader.
#
# Database
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/kitenge
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=CHANGE_ME
SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver
PORT=8080

# JWT (use a strong 32+ char secret)
JWT_SECRET=CHANGE_ME_CHANGE_ME_CHANGE_ME_CHANGE_ME

# Email (Gmail: use an App Password, not your normal password)
# Supported env var names:
# - Preferred: SPRING_MAIL_USERNAME / SPRING_MAIL_PASSWORD
# - Also supported: EMAIL_USER / EMAIL_PASS
# If you're hosting on Render free (SMTP blocked), use EMAIL_WEBHOOK_URL instead.
SPRING_MAIL_USERNAME=your_email@gmail.com
SPRING_MAIL_PASSWORD=YOUR_GMAIL_APP_PASSWORD
EMAIL_USER=
EMAIL_PASS=
APP_MAIL_FROM=your_email@gmail.com
EMAIL_WEBHOOK_URL=

# Admin
ADMIN_EMAIL=admin@example.com
ADMIN_NOTIFICATION_EMAIL=admin@example.com
ADMIN_DEFAULT_PASSWORD=
ADMIN_WHATSAPP=2507XXXXXXXX

# Frontend + CORS
APP_FRONTEND_URL=http://localhost:3000
APP_CORS_ALLOWED_ORIGINS=http://localhost:4000,http://localhost:3000

# File storage
FILE_UPLOAD_DIR=uploads
FILE_STORAGE=auto

# Cloudinary (optional)
CLOUDINARY_URL=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=

# WhatsApp API options (optional)
CALLMEBOT_API_KEY=
GREEN_API_ID_INSTANCE=
GREEN_API_TOKEN=
CHATAPI_INSTANCE=
CHATAPI_TOKEN=

# SMS (optional)
# Your backend will POST JSON: { to, from, message } to this URL.
SMS_WEBHOOK_URL=
SMS_SENDER=KitengeBora

# Base URL (used in messages)
BASE_URL=http://localhost:8080
