# Development Profile Configuration
# Use with: --spring.profiles.active=dev

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/kitenge}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1930}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
spring.jpa.show-sql=true
logging.level.com.kitenge=DEBUG
logging.level.org.springframework.security=DEBUG

# Admin defaults
app.admin.email=${ADMIN_EMAIL:jojogroceries@gmail.com}
app.admin.notification.email=${ADMIN_NOTIFICATION_EMAIL:jojogroceries@gmail.com}
app.admin.default-password=${ADMIN_DEFAULT_PASSWORD:Admin@123}
app.admin.force-password-reset=true

